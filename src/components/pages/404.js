import React from 'react';

const pageNotFound = () => (
  <React.Fragment>
    <h1 style={{ textAlign: 'center', margin: '60px' }}>
      404 Not found!
      <br />
      Page Doesn't Exist
    </h1>
  </React.Fragment>
);

export default pageNotFound;
